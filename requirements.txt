# Reproducibility environment for the SALI analysis code.
#
# The original archived environment pinned exact versions (see AUDIT.md, item C1):
#   pandas==2.3.3  numpy==2.4.6  scikit-learn==1.9.0  scipy==1.18.0
#   matplotlib==3.11.0  shap==0.52.0  lightgbm==4.7.0  xgboost==3.4.0
#   python-docx==1.2.0  python-pptx==1.0.2  openpyxl==3.1.5
# Those pins are deliberately relaxed here to >= so the code can be installed on a
# current Python 3.11-3.13 environment. If you need bit-for-bit parity with the
# archived run, install the pinned versions above instead.

pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
scipy>=1.12
matplotlib>=3.8
shap>=0.45
lightgbm>=4.3
xgboost>=2.0
python-docx>=1.1
openpyxl>=3.1
